# Dictionary for `ospsuite` PK parameters defined by Configuration Plan

Dictionary for `ospsuite` PK parameters defined by Configuration Plan

## Usage

``` r
pkDictionaryQualificationOSP
```

## Format

An object of class `list` of length 33.

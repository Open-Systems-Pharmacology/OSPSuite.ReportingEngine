# checkMetaDataIsConsistent

Check consistency of metadata units, dimensions and residuals scale

## Usage

``` r
checkMetaDataIsConsistent(metaData)
```

## Arguments

- metaData:

  A data.frame summarizing meta data of multiple `SimulationSet` and
  `Output` objects

# tlfLinetype

Translate value of `LineStyle` property from configuration plan into tlf
`linetype`

## Usage

``` r
tlfLinetype(configurationLinetype)
```

## Arguments

- configurationLinetype:

  Value of the configuration plan plot property

## Value

`linetype` values that follows `tlf` package nomenclature

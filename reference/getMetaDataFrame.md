# getMetaDataFrame

Get time profile list of metaData as a single data.frame

## Usage

``` r
getMetaDataFrame(listOfMetaData)
```

## Arguments

- listOfMetaData:

  list including `Path` and `Concentration`

## Value

A data.frame with variables `path`, `dimension`, `unit`, `group`,
`color`, `fill`

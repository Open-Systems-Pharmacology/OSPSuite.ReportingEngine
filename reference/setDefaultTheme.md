# setDefaultTheme

Set the default plot settings for a workflow

## Usage

``` r
setDefaultTheme(theme = NULL)
```

## Arguments

- theme:

  `Theme` object from `tlf` package If `NULL`, the current theme is
  re-initialized to the reporting engine default

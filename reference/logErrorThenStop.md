# logErrorThenStop

Log the error with a message and then stop, displaying same message.

## Usage

``` r
logErrorThenStop(message, logFolder = NULL)
```

## Arguments

- message:

  message to display and then log

- logFolder:

  **Deprecated** path where logs are saved

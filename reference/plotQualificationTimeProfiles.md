# plotQualificationTimeProfiles

Plot time profile for qualification workflow

## Usage

``` r
plotQualificationTimeProfiles(configurationPlan, settings)
```

## Arguments

- configurationPlan:

  A `ConfigurationPlan` object

- settings:

  `ConfigurationPlan` object

## Value

list with `plots` and `tables`

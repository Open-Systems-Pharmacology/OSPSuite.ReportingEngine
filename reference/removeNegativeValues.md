# removeNegativeValues

removeNegativeValues

## Usage

``` r
removeNegativeValues(data, dataMapping = NULL)
```

## Arguments

- data:

  data.frame

- dataMapping:

  name of variable on which the missing values are checked

## Value

filtered data.frame

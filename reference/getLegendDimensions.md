# getLegendDimensions

Get legend dimensions

## Usage

``` r
getLegendDimensions(plotObject)
```

## Arguments

- plotObject:

  A `ggplot` object

## Value

A list of `width` and `height` if legend is found, otherwise `NULL`

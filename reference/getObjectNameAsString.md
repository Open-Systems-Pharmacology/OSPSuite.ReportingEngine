# getObjectNameAsString

Return the name of an object as a string

## Usage

``` r
getObjectNameAsString(object)
```

## Arguments

- object:

  the name of which is to be returned

## Value

the name of the `object` as a string

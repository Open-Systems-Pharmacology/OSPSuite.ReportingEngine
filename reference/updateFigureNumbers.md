# updateFigureNumbers

Update figure captions and references in report

## Usage

``` r
updateFigureNumbers(fileContent)
```

## Arguments

- fileContent:

  Content of a markdown or text file read as an array of character
  strings

## Value

Array of character strings

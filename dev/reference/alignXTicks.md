# alignXTicks

Use the `plotConfiguration` of a `plotObject` to check and perform
vertical alignment of x-axis tick labels

## Usage

``` r
alignXTicks(plotObject)
```

## Arguments

- plotObject:

  A `ggplot` object

## Value

A `ggplot` object

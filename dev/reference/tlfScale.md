# tlfScale

Translate value of `Scaling` property from configuration plan into tlf
`scale`

## Usage

``` r
tlfScale(configurationScale)
```

## Arguments

- configurationScale:

  Value of the configuration plan plot property

## Value

`scale` values that follows `tlf` package nomenclature

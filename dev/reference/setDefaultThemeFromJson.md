# setDefaultThemeFromJson

Set the default plot settings for a workflow from a json file

## Usage

``` r
setDefaultThemeFromJson(jsonFile)
```

## Arguments

- jsonFile:

  path to json file that includes `Theme` properties to be loaded

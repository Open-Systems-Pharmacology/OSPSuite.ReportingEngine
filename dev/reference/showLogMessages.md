# showLogMessages

Display log messages as a data.frame

## Usage

``` r
showLogMessages(logTypes = LogTypes)
```

## Arguments

- logTypes:

  Select specific logs in `LogTypes` that will be displayed in the
  data.frame.

# qualificationCatch

Add configuration plan plot information when catching a warning/error

## Usage

``` r
qualificationCatch(expr, configurationPlanField = NULL)
```

## Arguments

- expr:

  Evaluated code chunks

- configurationPlanField:

  A list extracted from a configuration plan field

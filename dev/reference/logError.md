# logError

Save error messages into a log error file

## Usage

``` r
logError(message, printConsole = NULL)
```

## Arguments

- message:

  message to save in log file

- printConsole:

  logical to print error on console

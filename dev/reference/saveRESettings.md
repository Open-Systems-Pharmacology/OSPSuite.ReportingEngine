# saveRESettings Save the current reporting engine global settings in a `.RData` file

saveRESettings Save the current reporting engine global settings in a
`.RData` file

## Usage

``` r
saveRESettings(file)
```

## Arguments

- file:

  `.RData` file containing the settings

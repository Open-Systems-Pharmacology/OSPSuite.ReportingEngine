# loadSimulationOnCores

Send structureSet to core, check its simulation has been loaded
successfully

## Usage

``` r
loadSimulationOnCores(structureSet)
```

## Arguments

- structureSet:

  containing simulationSet which contains path to simulation file and
  pathIDs to be loaded in simulation object as outputs

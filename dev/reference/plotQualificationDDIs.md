# plotQualificationDDIs

Plot observation vs prediction for DDI qualification workflow

## Usage

``` r
plotQualificationDDIs(configurationPlan, settings)
```

## Arguments

- configurationPlan:

  A `ConfigurationPlan` object

- settings:

  A `TaskSettings` object

## Value

list of qualification DDI ggplot objects

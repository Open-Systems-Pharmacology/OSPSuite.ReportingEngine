# getRESettings

Get properties from name of the default/global settings stored in
`reEnv`.

## Usage

``` r
getRESettings(settingName)
```

## Arguments

- settingName:

  setting name as defined in enum `reSettingsNames`

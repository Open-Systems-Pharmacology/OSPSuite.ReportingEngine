# formatAxisProperties

Format configuration plan field values to a list compatible with `tlf`
package nomenclature

## Usage

``` r
formatAxisProperties(axisField)
```

## Arguments

- axisField:

  List of configuration plan field properties

## Value

A list of properties compatible with `tlf` package nomenclature

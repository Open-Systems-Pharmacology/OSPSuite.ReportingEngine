# setQuadraticDimension

Set quadratic dimensions if plot configuration is not user-defined

## Usage

``` r
setQuadraticDimension(plotObject, plotConfiguration = NULL)
```

## Arguments

- plotObject:

  A `ggplot` object

- plotConfiguration:

  `PlotConfiguration` object defined in task settings

## Value

A `ggplot` object

# tlfShape

Translate value of `Symbol` property from configuration plan into tlf
`shape`

## Usage

``` r
tlfShape(configurationShape)
```

## Arguments

- configurationShape:

  Value of the configuration plan plot property

## Value

`shape` values that follows `tlf` package nomenclature
